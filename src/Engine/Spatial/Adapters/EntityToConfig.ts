import type { TSpatialGridConfig, TSpatialGridWrapper } from '@/Engine/Spatial/Models';

export function entityToConfig(entity: TSpatialGridWrapper): TSpatialGridConfig {
  // TODO 15-0-0: implement
  // TODO 15-0-0: Not sure if we need an adapter for SpatialCellWrapper

  return {};
}
