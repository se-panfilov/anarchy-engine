import type { TLocaleName } from '@Anarchy/i18n';
import { enUs, nlNl } from '@Anarchy/i18n';

export const ShowcasesLangs: Record<TLocaleName, TLocaleName> = {
  [enUs.id]: enUs.id,
  [nlNl.id]: nlNl.id
};
